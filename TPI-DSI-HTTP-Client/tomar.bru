meta {
  name: tomar
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/eventos/tomar-evento
  body: json
  auth: inherit
}

body:json {
  { "eventoId": 1 }
}

settings {
  encodeUrl: true
  timeout: 0
}
